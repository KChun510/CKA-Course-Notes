# How to set a env var

## Using pod def var

- Use the `env` prop.
    - Basic manual, key val map

- Can use cofig maps, and secret keys.

- Once keys set, they become avail to all containers whithin pod.
