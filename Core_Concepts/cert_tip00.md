# Mid exam, difficult to manually write yaml file?
- You can get away with `kubectl run` command.

- I.e: If you were asked to create a pod or deployment with a sepcfic name and image, you can simply run the `kubectl run` command.


