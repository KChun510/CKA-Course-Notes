# ClusterIP
- Can group pods, into a service clusterIP and provide a single interface for other pods to access the services.

- DB, cluster gets it own IP/Interface
- Front End, cluster gets its own interface
- etc. 

