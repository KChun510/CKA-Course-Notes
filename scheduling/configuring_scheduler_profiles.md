# Stages of scheduling
1) Sched Que.
2) Filtering
3) Scoring
4) Binding

- Each stage is done via a `plugin`, that can be configured in thes scheduling profile.
- We can swap out the `plug in`, via an `exentension` point.
