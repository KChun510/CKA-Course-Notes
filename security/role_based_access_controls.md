# Creating roles
- TLDR: Its another resource def.
    - Look at kubernetes doc for standard yaml def.
- You can restrict access down the `NAME` of the resource, not just the typ of the resource, not just the type.

# RoleBinding
- Need to create another yaml resourece
    - Binding the user to pre-defined roles.
