# Container Sec
- Define a set of sec standards

# Kubernetes sec
- Can configure sec at a pod or container level.
    - If you define at a pod level, all standards applied to containers whithin.
    - If you set standards at the container level, then apply pod config.
        - Container level config, overwrites pod config.

