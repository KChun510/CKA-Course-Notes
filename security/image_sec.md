# Image names
- I.e:
    - Yaml def:
    ```yaml
    image: library/nginx
    ```
    - Format:
    ```yaml
    image: docker.io(registery)/library(User/Acccount)/nginx(image/repository)
    ```

- GCP & AWS:
    - Offer private registery services

- Know you Docker basics/ fundementals

## Workflow/ Setup:
- `kubectl create secret docker-registery regcred \ --options.....`
