# Monitoring/ Resource Consumption
- Metric Server, Per cluster. 
    - Stores node and pod data in `mem`.
    - Need something for long term snap shots, use open source/ existent sols.

- There is a central server, that all metric servers send their information too.
